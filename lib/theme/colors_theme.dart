import 'package:flutter/material.dart';

const primaryColor = Color(0xFFED6A25);
const secondaryColor = Color(0xFF4B5481);
const thirdColor = Color(0xFFEFF7FF);
const backgroundColor = Color(0xFFFFFFFF);
const titleColor = Color(0xFF08090D);
const bodyColor = Color(0xFF999AA9);
const placeholderColor = Color(0xFFA8B2D3);

const btnBackgroundGradient = LinearGradient(
  colors: [primaryColor, Color(0xFFFF9A68)],
  end: Alignment.topCenter,
  begin: Alignment.bottomCenter,
);

const compBackgroundGradient = LinearGradient(
  colors: [Color(0xFFBCDCFC), Color(0xFFEFF7FF)],
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
);
